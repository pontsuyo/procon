#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i,n) for(int i=0;i<n;i++)

struct vertex{
    vector<vertex*> edge;

    /*
    頂点の属性
    */
};

vertex G[MAX_V];

int main(){
    return 0;
}