#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define rep(i,n) for(ll i=0;i<n;i++)

int main(){
    int n; cin >> n;
    vector<int> a(n), b(n), c(n), d(n);
    rep(i, n){
        cin >> a[i] >> b[i];
    }
    rep(i, n){
        cin >> c[i] >> d[i];
    }
    
    return 0;
}