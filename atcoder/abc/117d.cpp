#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i,n) for(int i=0;i<n;i++)

int main(){
    ll n, k;
    cin >> n >> k;
    ll a;
    ll tmp;
    rep(i, n){
        cin >> tmp;
        a.emplace_back(tmp);
    }

    
    return 0;
}