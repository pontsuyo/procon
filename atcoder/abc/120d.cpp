#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef pair<int, int> P;
#define rep(i,n) for(int i=0;i<n;i++)

int main(){
    int N, M;
    cin >> N >> M;
    int A[M], B[M];
    rep(i, M){
        cin >> A[i] >> B[i];
    }


    printf("%d\n", n);
    return 0;
}