#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef pair<int, int> P;
#define rep(i,n) for(int i=0;i<n;++i)
#define MOD (int) 998244353

int main(){
    int n;
    cin >> n;
    vector<int> x(n), y(n);
    rep(i, n){
        cin >> x[i] >> y[i];
    }
    
    
    
    cout << n << endl;
    // printf("%d\n", N);
    return 0;
}