#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i,n) for(int i=0;i<n;i++)

int main(){
    string s;
    int x, y;
    cin >> s >> x >> y;

    vector <int< idx;
    vector <int> dx;
    vector <int< dy;

    rep(i, s.size()){
        if(s[i] == 'T'){

    }
    return 0;
}