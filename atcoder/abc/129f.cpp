#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef pair<int, int> P;
#define rep(i,n) for(int i=0;i<n;i++)

int main(){
    ll L, A, B;
    int M;
    cin >> L >> A >> B >> M;
    int keta;

    int ans;
    rep(i, L){
        ans += 
    }

    printf("%d\n", n);
    return 0;
}