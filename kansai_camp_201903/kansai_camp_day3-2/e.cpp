#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i,n) for(int i=0;i<n;i++)

int main(){
    int n;
    double p, s, v;
    cin >> n >> p >> s >> v;

    n*pow(log2(n), 
    return 0;
}